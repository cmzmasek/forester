# Compiled Java class files — build output, regenerated by the build; never commit.
*.class

# Build output directories (e.g. Ant's forester/java/classes, IDE output dirs).
**/classes/
bin/
out/

# Native-installer output from the jpackage Ant targets (large; regenerated on demand).
**/dist/
*.dmg
*.pkg

# Local working notes for Claude Code — never commit.
CLAUDE.md
